<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:extensions="clr-namespace:MarkdownWPF.Extensions;assembly=MarkdownWPF"
    xmlns:regions="clr-namespace:MarkdownWPF.Models.Regions;assembly=MarkdownWPF"
    xmlns:renderer="clr-namespace:MarkdownWPF.Renderer;assembly=MarkdownWPF"
    xmlns:sys="clr-namespace:System;assembly=mscorlib">

    <!--  Here datatemplates  -->

    <renderer:MarkdownRenderer x:Key="MdRenderer" />

    <DataTemplate DataType="{x:Type regions:ParagraphRegion}">
        <TextBlock
            Margin="0,10,0,5"
            extensions:TextBlockExtensions.MarkdownRenderer="{StaticResource MdRenderer}"
            extensions:TextBlockExtensions.Source="{Binding Elements}"
            TextWrapping="WrapWithOverflow" />
    </DataTemplate>


    <DataTemplate DataType="{x:Type regions:HeadingRegion}">
        <TextBlock
            Margin="0,10,0,5"
            extensions:TextBlockExtensions.MarkdownRenderer="{StaticResource MdRenderer}"
            extensions:TextBlockExtensions.Source="{Binding Elements}"
            TextWrapping="WrapWithOverflow">

            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="FontSize" Value="40" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Level}" Value="2">
                            <Setter Property="FontSize" Value="32" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Level}" Value="3">
                            <Setter Property="FontSize" Value="24" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Level}" Value="4">
                            <Setter Property="FontSize" Value="16" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Level}" Value="5">
                            <Setter Property="FontSize" Value="14" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Level}" Value="6">
                            <Setter Property="FontSize" Value="12" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>
    </DataTemplate>

    <DataTemplate DataType="{x:Type regions:CodeRegion}">
        <Border
            Padding="9.5"
            Background="#f8f8f8"
            BorderBrush="#333"
            BorderThickness="1">
            <ItemsControl ItemsSource="{Binding Elements}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock
                            FontSize="14"
                            Foreground="#333"
                            Text="{Binding Text}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Border>
    </DataTemplate>
</ResourceDictionary>